stages:
  - test

# Простая проверка - выполняется на раннере
hello-world:
  stage: test
  script:
    - echo " Pipeline работает!"
    - echo ": $(hostname)"
    - echo ": $(whoami)"
    - echo ": $(uname -a)"
    - echo ": $(date)"
    - echo "✅ Всё отлично!"